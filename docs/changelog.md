# 끝난 것

**추가만 한다.** 지우지도 고치지도 않는다. 현황판이 지금 상태를 갖고 여기가 지나온 것을 갖는다.

한 줄에 하나다. 결함을 닫은 것이면 `W-NN`을 부르고, 그러면 `git log -- docs/issues/W-NN.md`로 전문을 되찾을 수 있다.

사건을 여기 적는 까닭은 스킬과 현황판에서 빼기 위해서다. 규칙이 왜 생겼는지가 필요하면 여기를 본다.

---

## 2026-09

- 저장소를 새로 열고 원본 v5.95를 서브모듈로 붙였다. 원본 위에 커밋을 얹던 방식을 버리고 한국어를 빌드 단계에서 조립하기로 했다 (D-01, D-02).
- 한국어 문장 대장 801행과 게임 용어 130개, 사용자 판정 24건을 옮겼다. 아홉 파일 전부 원본과 한 자도 다르지 않은 것을 대조로 확인했다.
- 기존 저장소가 원본 파일에 직접 넣어 두고 있던 개선분 셋을 버리고 `upstream-reports/`에 옮겨 적었다. 원본의 결함은 우리가 고치지 않는다 (D-06).
- 한국어 작업 스킬 셋을 923줄에서 476줄로 줄였다. 사건 서술을 빼고 규칙만 남겼으며, 지우면 사라지는 판정은 `korean/lexicon.json`과 `docs/dev/`로 옮겼다.
- 배포 창구를 정했다. 기존 저장소는 `repo.json` 하나만 남기고 실물은 새 저장소 릴리스에 둔다. 이미 깐 사용자가 아무것도 안 해도 새 버전을 받는다 (D-08).
- 조립하다가 원본 v5.95의 인코딩 손상을 찾았다. `YesNoLabels`의 일본어 라벨 둘이 물음표로 바뀌어 있다. 고치지 않고 `upstream-reports/`에 적었다.
- 파서를 두 번 넓혀 못 읽던 자리 43곳을 13곳으로 줄였다. 보간 자리 안의 따옴표에서 24곳, 중첩 삼항을 펴서 6곳이다. **"못 읽음"을 파서의 한계로만 다뤄 왔는데 그것이 사용자가 듣던 영어였다.** 고아로 잡혀 있던 `Levequests` 둘도 결함이 아니라 파서가 못 읽은 것이어서, 펴자마자 물렸다.
- 열린 41곳을 옮겨 미적용을 45곳에서 4곳으로 줄였다. 대장 842행, 적용 862곳이다. 남기는 넷은 어순이 걸리는 `CounterConnector`, 낱말이 없는 `TargetDirection`·`GaugeAttunement`, 게임에 낱말이 없는 `DCSelected`다.
- 대장 값이 보간 자리 안에 escape 없는 따옴표를 담을 수 있게 됐다. 그래서 대장이 처음으로 컴파일을 깨뜨릴 수 있게 되어, 중괄호 균형·중첩 리터럴 정합·`references` 대조 셋을 막는 장치로 넣었다.
- 용어 도구가 `Addon` 하나에서 `Action`·`Pet`·`Status`까지 넷을 읽는다. 소환사 게이지 용어 일곱이 전부 `Action`에 있었다. **Addon에 없다는 것은 "게임에 없다"가 아니라 "이 도구가 보는 범위 밖"이었다** — 기존 `not_found` 셋도 그렇게 사실이 아니었다.
- 인게임에서 실증했다. 미번역 로그가 12건 7종에서 1건 1종으로, 발화 282건 중 한국어가 아닌 것이 0건이 됐다(숫자뿐인 `0/140` 하나 제외). 장소 유형이 독일어로 들리는 것은 원본 결함이라 그대로 두고 `upstream-reports/`에 적었다.
- 표식 밖에 있던 사각지대 둘을 열어 적용이 862곳에서 984곳으로 늘었다. `ColorNamer.cs`가 별칭 `De`로 갈리는 **104곳**은 적용에도 미적용에도 안 잡힌 채 캐릭터 생성의 색상 묘사를 통째로 영어로 내보내고 있었다. 방위 낱말 **18곳**은 언어별 배열을 통째로 고르는 자리라 파서가 배열 이름만 보고 낱말은 못 봤고, `{direction}` 홀을 가진 한국어 문장 27건에 `North`가 들어가고 있었다. 배열 셋을 원소별 갈림길로 펴서 열었다.
- 첫 릴리스 `v5.95.0.0`을 냈다. 도로 받아 재계산한 해시·버전·주소가 맞고 받는 쪽 주소 넷이 전부 200에 크기까지 맞았다.
- 원본 v5.96을 얹고 `v5.96.0.0`을 **CI에서** 발행했다. 새로 온 문장 61곳을 전부 옮겨 미적용이 얹기 전 수준으로 돌아갔고, 원본이 들여온 자기 갱신은 덧대기 둘로 우리 릴리스를 보게 맞췄다. 발행 경로가 로컬 명령 하나뿐이던 것을 워크플로로 옮겼다.
- 파서가 못 읽던 갈래 다섯을 펴서 열었다. 이어붙인 리터럴은 **영어로 나가면서 미적용에도 안 잡혀** 세지도 고치지도 못하는 자리였다. 교차 리뷰가 그중 둘을 짚었다 — 비행 종료 안내는 높낮이가 5미터를 넘을 때만, 지역 이동 안내는 영어가 단복수를 문장 안 삼항으로 고르는 탓에 한국어 문장에서 `transition`이 발화됐다.
- 원본 여덟 버전(v5.97~v6.08.8)을 한 번에 얹었다. 태그가 붙은 것은 v6.08.5부터 넷뿐이고 나머지는 개발자가 비공개로 돌린 분량이 통째로 들어왔다. 커밋 29개, 파일 153개다. 새 문장 70곳을 전부 옮겨 잔량이 얹기 전으로 돌아갔다 — 미적용 4곳, 못 읽음 10곳, 고아 3행이다.
- 원본이 버전 번호를 세 마디로 적고 가운데에 앞자리 0을 붙이기 시작해 우리 버전 파이프라인 세 곳이 깨졌다. 조립이 앞자리 0을 지워 찍고, 발행이 원본 태그를 우리 버전 번호에서 역산하는 대신 서브모듈 핀에서 직접 읽는다 (D-11).
- **한 번에 여러 버전을 얹으면 원본 노트도 여럿이다.** 발행이 직전 릴리스 태그의 gitlink에서 옛 핀을 읽어 그 뒤에 붙은 태그를 전부 받아 합친다. 그 태그가 로컬에 없으면 하나만 받아 온다 — CI가 발행한 태그는 원격에만 있고, 받아 오지 않으면 커버리지를 한 버전만 재면서 화면에는 아무것도 안 뜬다.
- 못 읽음 둘을 펴서 닫았다. 적 색상 여덟은 언어별 배열이라 파서 밖이었고 전투마다 발화되는 자리다. 마테리아 문장은 조립이 중첩 삼항을 편 뒤 양쪽이 글자까지 같은 죽은 갈림길이 남은 것이라 안쪽 조건을 손으로 밖에 뺐다.
- 힐 모니터가 한국판 달라무드에 없는 게임 API 넷을 써서 빌드가 섰다. 있는 배열의 길이로 대신하고 `DisplayRow`는 원본이 정의해 둔 폴백으로 떨어뜨렸다. 원본 결함이 아니라 환경 차이라 보낼 목록에 안 올린다 (D-12).
- 힐 모니터의 소리 시험 문구를 '거의 사망'으로 썼는데 게임이 그 낱말을 안 쓰고 '전투불능'만 쓴다는 것이 낱말 검사에서 드러났다. **검사가 없었으면 지어낸 낱말이 그대로 나갔다.**
- 압축 검사가 폴더를 무조건 막고 있어서 힐 모니터 음성 mp3 122개가 든 배포물을 아예 못 만들었다. 원본 릴리스 압축을 받아 열어 보니 원본도 `assets/partymonitor/`를 그대로 담아 배포하고 있었다. 폴더를 통째로 열지 않고 이름 모양까지 재는 패턴으로 통과시켰다 — 화이트리스트가 이 검사의 요점이라 폴더 하나를 열면 그 아래로는 무엇이든 들어온다.
- `v6.8.8.0`을 CI에서 발행했다. 자산 넷이 다 올라갔고 도로 받아 재계산한 해시·버전·주소가 맞다.
- 한국판 달라무드를 15.0.3.3으로 올리니 **FFXIVClientStructs 7.51 다운그레이드가 끝나 있었다.** 업데이터 0.5.1부터 공식이 싣는 버전(7.55.1.9032)을 그 자리에서 패치한다. 예약해 둔 조건이 그대로 충족되어 힐 모니터 덧대기 넷을 걷어냈고 원본 코드가 그대로 나간다 (D-12 해제).
- 업데이터를 GUI 없이 돌리는 길을 찾았다. `Dalamud.Updater.Gui.exe --patch-hook`이 KR 패치를 씌우고 자체 검증까지 한다. **다만 다른 업데이터 창이 떠 있으면 `SingleInstance` 뮤텍스에 막혀 아무 일도 없이 종료 0으로 끝난다** — 처음에 그것에 걸려 패치가 안 된 것을 성공으로 읽을 뻔했다.
- 빌드 참조의 「넷은 바이트가 같다」가 **같은 어셈블리 버전끼리만 성립한다.** 공식이 15.0.3.3으로 오른 뒤 15.0.3.2 설치본과 재니 넷이 다 달랐고, 설치본을 올린 뒤 다시 재니 다시 다 같았다. 버전이 어긋난 채로 재면 표를 고쳐야 한다는 잘못된 결론에 이른다.
- 용어 대장의 `row_text` 셋이 게임 업데이트(7.55→7.56)으로 늘어난 문장과 어긋나 대조 검사가 빨갰다. 낱말과 행 번호는 맞았으므로 잘못 나가던 번역은 없었지만, **빨간 채로 두면 진짜 낱말 변화가 그 셋에 묻힌다.** 덤프를 다시 뜨고 맞췄다.
- 게임 시트에 없는 조합어(`채집 지점`)가 대장의 자기 검증에 걸려 CI가 하루 빨갰다. 낱말을 바꾸지 않고 재료를 적는 자리를 두어, 그런 줄은 재료가 시트에 있는지를 대신 재게 했다. **그 예외가 면죄부가 되지 않는 것도 같이 검사한다.**
- `v6.8.8.1`을 CI에서 발행했다. 도로 받아 잰 해시·버전·주소가 맞고 받는 쪽 주소 넷이 전부 200이다.
- 공식 달라무드가 15.0.3.4(FFXIVClientStructs 7.55.1.9047)를 내면서 **한국판 업데이터 0.5.1의 [업데이트 확인]이 검증에서 선다.** 업데이터는 내장 호환 파일이 7.55.1.9032짜리 하나뿐이고, kamori의 최신 하나만 보므로 **특정 버전을 고를 인자도 설정 필드도 없다** — 명령줄 스위치 여덟 개를 전수로 확인했다. 사용자가 우회할 방법이 없어 MiqoKR이 새 버전을 내야 한다.
- **그 실패가 아무것도 안 망가뜨린다.** 내려받기와 검증이 `%TEMP%`에서 끝나고 프로필을 건드리는 `DirectoryReplacement::Activate`가 예외 지점보다 뒤에 있다. 그래서 안내를 "누르지 마라"가 아니라 "눌러도 안 망가진다"로 적었다. **겁주는 금지와 사실 안내는 사용자가 할 일이 다르다.**
- **막히는 것은 이미 깔린 사람이 아니라 새로 까는 사람이다.** 15.0.3.3이 있으면 게임도 모드도 정상이고, 달라무드가 없거나 15.0.3.2에 머물면 받을 길이 통째로 없어진다. 한 줄로 "누르지 마라"만 적으면 뒤의 두 무리에게 틀린 안내가 된다.
- 한국 서버용 달라무드 한 벌을 `dalamud-kr-full-15.0.3.3`으로 발행했다(81MB, prerelease). 업데이터가 `--patch-hook`으로 검증을 통과시켰고, 도로 받아 잰 해시가 맞고 `latest`도 안 뺏겼다 (D-13).
- **pdb 74MB를 빼도 안전하다는 것을 실증했다.** `hashes.json` 84개 항목에 pdb가 하나도 없고, 뺀 상태로 `--patch-hook`을 돌리니 `VerifyCoreCompatibility`가 참조를 전부 해석했다. 크기가 199MB에서 압축 81MB가 됐다.
- 업데이터를 격리해 돌리는 길을 찾았다. `KR_DALAMUD_SETTINGS_PATH`가 설정 경로의 1순위이고 계산하는 자리가 `get_SettingsPath` 하나뿐이라 `.bak`까지 같이 갈라진다. **다만 부트스트래퍼 `Dalamud.Updater.exe`를 쓰면 안 된다** — `StartGui`가 그 환경변수를 메인 경로로 무조건 덮어써서 격리가 깨진다.
- `README.ko.md`의 단축키 목록 링크가 `KEYS.ko.md`를 가리켜 GitHub 웹에서 404였다. **윈도는 대소문자를 안 가려 로컬에서는 원리적으로 안 드러난다.**
- 한 벌을 격리 프로필에 풀고 설치 프로그램을 돌려 **끝에서 끝을 실증했다.** 달라무드를 준비됨으로 읽고, 업데이터를 안 부르고, 15분 대기 없이 모드가 깔린다. 사용 안내가 적은 절차가 실제로 그렇게 도는 것을 확인했다 — 남은 것은 인게임 귀 판정 하나다.
- 그 과정에서 `W-01`을 찾았다. `KrPatchedHookFolder()`가 마커 가진 **첫** 폴더에서 멈춰서, 후크 폴더가 둘인 머신에서 `--check`가 실제로 쓰이는 15.0.3.3이 아니라 15.0.3.2를 지목한다. 준비 여부는 맞고 **지목하는 이름이 틀린다.** 마커를 패턴으로 둔 것은 의도이므로 패턴을 좁히지 말고 고를 기준을 더해야 한다.
- `MiqoKR/kr-dalamud-updater#3`으로 보고했다. **고칠 자리를 지목하는 절은 사용자 판정으로 뺐다** — 그쪽이 자기 코드를 훨씬 잘 알고, 그 정보는 우리가 바이너리를 뜯어 얻은 것이라 내미는 모양도 좋지 않다. 남긴 것은 버전 번호와 오류 문구 원문과 누가 막히는지뿐이다. **우리가 한 벌을 올린 것도 그 이슈에 먼저 밝혔다.**
- **설치 프로그램이 달라무드를 직접 받아 넣게 했다** (`v6.8.8.2`). 문서로 "압축을 이 폴더에 푸세요"를 시키는 것은 화면을 못 보는 사용자에게 실패하기 쉬운 단계이고, **실제로 만든 사람이 그 순서를 문서에 틀리게 적었다.** 받을지 먼저 묻고 해시를 맞춰 본 뒤에 푼다.
- 그 과정에서 순서 함정을 찾았다. `InstallerService.cs:343`이 달라무드가 있으면 그 자리에서 돌아 나가 **업데이터를 아예 안 깐다.** 달라무드를 손으로 넣은 사람은 업데이터 없이 끝나고 모드가 게임에 안 붙는다. 업데이터 설치를 달라무드 검사보다 앞으로 옮겼다.
- **브랜치를 따로 안 뒀다.** `release.yml`이 기본 가지에서만 돌아 브랜치에서는 발행이 안 되고 되돌리기도 더 어렵다. D-12가 쓴 방법 그대로 **로직은 `kr/`에 새 파일 하나, 배선은 graft 규칙 둘**로 넣었다 — 지울 때 파일과 규칙 이름만 지우면 원본 흐름이 돌아온다. `replace/`도 원본 서브모듈도 안 늘렸다.
- `pack-check`가 `dist/` 루트에 남은 작업 폴더를 잡았다. **검사를 열지 않고 폴더를 치웠다** — 그 검사가 보는 것이 곧 사용자가 받는 자리다.
- **참조 릴리스 `dalamud-kr-15.0.3.3`에 자산 넷이 안 올라가 있었다.** `SHA256SUMS.txt`는 여섯을 적는데 실제로는 `Dalamud.dll`과 `FFXIVClientStructs.dll` 둘뿐이었고, `Lumina.dll`·`Lumina.Excel.dll`·`InteropGenerator.Runtime.dll`·`ImGuiScene.dll`이 빠져 있었다. **이 저장소가 문서에 적어 둔 바로 그 실패 방식이다** — 자산이 반만 올라가도 `gh`는 0으로 끝나고, 그 릴리스를 되받아 재지 않아서 만든 날부터 몰랐다.
- 그것을 **발행 워크플로가 잡았다.** `sha256sum -c`가 넷을 FAILED로 내고 섰다. 빠진 넷을 올리고 되받아 재니 여섯이 다 OK다. 로컬 후크 폴더의 파일과 해시가 글자까지 같아서 다시 만들 필요는 없었다.
- **달라무드 설치에서 묻는 것을 없앴다** (`v6.8.8.3`). 업데이터를 받을지 물은 직후에 한 번 더 물어서 같은 자리에서 두 번 답해야 했다. **달라무드는 vnavmesh나 던전 경로와 달리 선택이 아니고** 사람은 이미 설치를 눌렀으므로, 두 번째 물음은 귀로 듣는 사람에게 단계만 하나 늘렸다. 설치 도중에 내던 설명 세 줄도 같이 걷어냈다 — 진행과 결과만 남는다.
- **끄는 길이 두 겹인 것을 코드에 적었다.** 받기가 실패하면 거짓을 돌려주고 원래 경로로 흘러가므로, **한 벌 릴리스를 내리는 것만으로 이미 나간 판에서도 이 기능이 꺼진다.** 아주 걷어낼 때는 `kr/` 파일 하나와 graft 규칙 둘을 지운다.
- **`v6.8.8.3` 발행.** 실증에서 받는 자리가 안내 문구보다 뒤에 있는 것을 잡았다 — 「업데이트 확인을 눌러주세요」를 말한 다음 조용히 알아서 받고 있었다. **한 줄씩 귀로 듣는 사람에게는 어긋나는 지시가 둘 남는다.** 받기를 앞으로 옮겨 실패했을 때만 그 안내가 나온다.
- **`v6.8.8.3`을 물리고 `v6.8.8.2`에 덮어썼다.** 둘이 받는 업데이트가 같아 버전을 나눌 이유가 없다는 사용자 판정이다. `v6.8.8.3` 릴리스와 태그를 지우고 개정 마디를 2로 되돌린 뒤 자산과 노트를 갈아 끼웠다. **버전 게이트를 `FF14_RELEASE_SAME_VERSION`으로 넘겼다** — 같은 번호로 다시 내면 이미 깐 사람에게 자동 갱신이 침묵으로 서는데, 나간 지 한 시간도 안 돼 해당자가 없다는 것을 먼저 확인하고 넘겼다.
- **노트 파일을 지우지 않는다는 `release-notes/README.md` 규약을 이번에 어겼다.** `6.8.8.3.md`를 `6.8.8.2.md` 자리로 옮겼다. `v6.8.8.3`이 릴리스에서 사라지므로 그 파일만 남기는 것이 더 나쁘다고 봤다.
- **덮어쓰면 태그가 나간 물건을 안 가리킨다.** `gh release edit`과 `upload --clobber`는 자산만 갈아 끼우고 태그는 처음 낼 때의 커밋에 그대로 둔다. 실제로 나간 코드는 그 뒤 커밋 넷이 더 얹힌 것이었다. 태그를 발행 커밋으로 강제로 옮겨 GitHub이 보는 값까지 맞췄다. **다음에 덮어쓸 때도 이 단계가 따로 필요하다** — 도구가 안 해 준다.
- 번역 문장 45행의 구두점을 원문에 맞췄다. 대장 1,085행을 독일어 원문과 전수 대조해 다른 109행을 가린 결과다. **목록 안내 13행은 끝 마침표까지 잃어서 음성이 다음 발화와 붙어 있었다.** 원문 구두점을 따르되 그 부호 용법이 독일어 고유 관습일 때만 예외로 두는 기준을 스킬에 세우고 검사로 막았다.
- **D-13을 해제했다** (`v6.8.8.4`). KR Dalamud Updater 0.5.2가 2026-09-11에 나와 공식 15.0.3.4를 다루므로, 업데이터가 막힌 동안 우리가 대신 열었던 조달 경로를 전부 걷어냈다. `kr/Installer/KrDalamudBundle.cs`와 graft 규칙 `installer-kr-bundle-offer`와 `Loc.cs`의 `KrBundle*` 여덟 키를 지웠고, 사용자 안내에서도 한 벌을 받는 6장 2절 ④와 그것을 가리키던 두 자리를 걷어냈다. **임시로 못 박고 해제 절차를 미리 적어 둔 것이 그대로 쓰였다** — 무엇을 지울지 찾아다니지 않았다.
- **`installer-kr-updater-first`는 같이 안 지웠다.** 해제 절차가 따로 판단하라고 못 박은 자리다. 그 규칙이 고치는 순서 함정은 한 벌 조달과 무관하게 성립하고, 같이 지우면 달라무드를 손으로 넣은 사람이 업데이터 없이 끝나는 결함이 되살아난다.
- **개정 마디 `3`을 건너뛰었다.** 한 번 내고 물린 번호라 같은 이름이 서로 다른 물건을 가리키게 된다. 넷째 자리는 업스트림이 안 쓰므로 건너뛰어도 원본 값과 부딪히지 않는다.
- **CI가 매일 빨개지는 구조를 걷어냈다.** 원인이 둘이고 둘 다 "우리가 통제 못 하는 것에 빨간 게이트를 건" 모양이었다. ① 동기화가 원본이 방금 낸 코드를 `-warnaserror`로 재서, 원본 `v6.08.20`의 죽은 필드 하나가 내는 경고 둘이 매일 06:00에 오류로 승격됐다 — **DLL은 실제로 만들어졌고 실패는 오직 경고 승격 때문이었다.** 원본의 결함은 우리가 고치지 않으므로 초록으로 가는 길이 없었다. ② 참조 어셈블리 넷을 공식 `latest.zip`에서 받아 못 박은 해시로 쟀다. 그 주소는 언제나 최신을 가리켜 공식이 버전을 올리면 모든 푸시와 모든 PR이 통째로 빨개진다(2026-09-10 여섯 건). 게이트는 진짜 오류만 보게 하고 경고는 세어서 PR 본문 맨 앞에 적으며, 어셈블리 여섯을 다 우리 참조 릴리스에서 받는다 (D-14, D-15).
- **그 둘이 잠복해 있던 것은 우연이었다.** 참조 릴리스에 `.dll` 여섯이 다 붙은 뒤로 액션의 두 번째 단계가 공식에서 꺼낸 넷을 덮어써서, 해시 대조가 우리 바이트를 우리 해시와 재는 항진명제가 돼 있었다. 그런데 빌드 참조 문서는 여전히 "자산은 두 `.dll`"이라고 적고 있어서, **문서대로 다음 참조 릴리스를 만들면 폭탄이 다시 장전되는 상태였다.**
- 같은 태그에 PR이 이미 열려 있으면 조립과 빌드를 다시 하지 않는다. 그전에는 매일 전부 다시 짓고 같은 가지에 새 커밋을 밀어 넣어서, 오류가 아니어도 매일 활동 알림이 되고 러너 시간을 매일 썼다.
- **현황판이 갖고 있던 「CI에서만 드러난 결함」 목록을 여기로 옮겼다.** 여섯에 넷을 더해 열이다. ① 트리거가 `branches: [main]`인데 기본 브랜치가 `master`라 워크플로가 아예 안 돌았다 ② 윈도 러너의 파이썬 stdout이 cp1252라 우리 도구가 한국어를 내는 순간 죽었다 ③ 저장소 설정의 "Actions가 PR을 만들 수 있음"이 꺼져 있어 동기화가 밀기까지 하고 PR 생성에서만 죽었다 ④ 워크플로 검사기가 PR 체크아웃에서 기본 가지를 "없는 브랜치"로 잡았다 ⑤ 배포 도구가 .NET SDK를 개발 머신의 스쿠프 경로로만 찾았다 ⑥ 설치 실증이 플레이 바로가기를 만드는데 러너에는 게임이 없다 ⑦ 공식 배포본이 버전을 올리면 거기서 꺼낸 넷이 우리 해시와 어긋난다 ⑧ 원본이 방금 낸 코드를 `-warnaserror`로 재서 우리가 못 고치는 이유로 매일 빨개진다 ⑨ 가지를 이름으로 체크아웃하면 로컬 git이 기본 가지를 몰라 멀쩡한 워크플로가 "없는 브랜치"로 걸린다 ⑩ `GITHUB_TOKEN`이 연 PR의 검사가 `action_required`로 멈춰, 승인 전에는 검사가 아예 안 붙은 것처럼 보인다. **공통점은 "저장소 밖 또는 러너 안의 사실"이라는 것이고**, 로컬에서 명령을 그대로 돌리는 실증으로는 원리적으로 안 잡힌다. ⑤⑥⑨는 그 실증조차 한 번에 못 잡았다.
- **⑨와 ⑩은 이번 확인 중에 나왔다.** ⑨는 동기화가 연 가지를 사람이 `workflow_dispatch`로 재다가 만났고, 원본 코드가 낸 진짜 빨강 둘 옆에 가짜가 섞여 **어느 것이 진짜인지 흐려졌다.** 검사를 무르지 않고 기본 가지 이름을 CI가 알려 주게 해서 풀었다 — 그 뒤에 또 한 번 밀어 보니 **실물을 재는 테스트만 여전히 빨갰다.** 임시 저장소를 재는 테스트를 위해 그 이름을 지우는 장치가 실물까지 막고 있었다.
- **원본 새 버전을 PR이 아니라 이슈 한 장으로 받게 바꿨다** (D-16). 서브모듈 포인터를 옮긴 가지를 밀고 PR을 여는 것을 걷어냈다. **그 변경은 병합 가능한 적이 없었다** — `v6.08.20`을 얹으면 미적용이 4곳에서 151곳이 되고 원본이 들여온 죽은 필드가 경고 둘을 내는데, 기본 가지는 `-warnaserror`로 빌드하므로 그대로 얹으면 빌드가 실패한 채로 남고 발행도 못 한다. 포인터를 옮기는 비용은 명령 한 줄이라 자동화할 값어치가 없고, 건너뛰기 조건이 최신 태그만 보므로 태그가 올라가면 PR이 쌓인다. **매일 재는 것은 그대로 뒀다** — 원본이 우리 덧대기 앵커를 깨뜨렸는지 그날 알고, 얹을지 판단할 재료를 미리 받는다.
- **옛 PR 본문이 378줄 27KB였다.** 새 미적용 147곳이 전부 나열돼 있어서 무엇을 봐야 하는지가 그 안에 묻혔고, 릴리스 노트·현황판 숫자·변경 기록처럼 **얹는 일의 대부분을 차지하는 문서 작업은 아무 데도 안 적혀 있었다.** `compare.py --limit`로 목록마다 15개까지만 적고 나머지를 개수로 말하게 했고, 「얹을 때 할 일」 아홉 단계를 본문에 넣었다. 같은 데이터로 82줄 4.2KB가 됐다.
- **원본 `v6.08.20`을 얹었다.** 조립이 서던 두 자리를 걷어냈다 — 원본이 들여온 `ChatPlayerService` 필드는 한 번도 만들어지지 않아 `-warnaserror` 빌드를 세우므로 조립 트리에서 뺐고(`plugin-drop-dead-chatplayer-field`), 파서가 못 읽던 넷을 펴서 못 읽음이 14곳에서 10곳으로 돌아왔다. 편 직후 미적용이 151곳에서 178곳으로 늘었고 그것이 옳은 방향이다.
- **조립 도구가 `Pick` 말고 다른 이름의 축약도 자리로 읽는다.** `GaugeReadyCues.cs`의 직업 이름 스물셋이 축약 `Job`으로 갈리는데, 선언은 조건만 갖고 리터럴은 호출부에 있어서 조립이 선언만 보고 못 읽음으로 넘겼다. 되쓸 때 그 자리가 쓰던 이름을 되쓴다 — 표식 별칭 `De`를 다루던 방식과 같다.
- **`Addon`에 직업 이름이 한 건도 없다.** 그 시트는 클래스 열여덟(806~823행)까지만 갖는다. 낱말 뽑는 도구가 `ClassJob` 시트도 읽게 해서 직업 이름 스물둘을 행 번호와 함께 대장에 올렸다. 그 시트의 앞 열여덟이 Addon의 클래스 목록과 글자까지 같은 차례라 행 번호가 곧 직업 번호인 것이 서로를 확인해 준다.
- **직업 게이지 자원 이름 예순하나와 점성술사 카드 여덟은 영어로 둔다.** 영어 쪽은 모드가 지은 말이 아니라 게임 자신의 낱말이고, 모드가 달라무드 `JobGauge`의 필드와 열거형을 그대로 읽어 쓴다. 한국어를 확정할 근거를 못 찾았다 — `Addon` 19,592행에 직업 게이지 라벨이 0건이고, 원본이 상수로 적어 둔 기술 번호 예순둘을 KR `Action` 시트에서 전수로 읽어도 나오는 것은 기술 이름이지 자원 이름이 아니다. **부정확한 이름은 사용자가 이름을 두 개 외우게 만든다**는 사용자 판정으로 원어를 택했고 릴리스 노트에 밝혔다.
- **`FATEs`가 형제 열다섯과 달리 맨 리터럴이던 것을 열었다.** 한국어 사용자가 `돌발 임무` 대신 `FATEs`를 듣고 같은 화면에서 두 이름이 섞이던 자리다. 대장의 고아 하나가 같이 닫혀 셋에서 둘이 됐다. 남은 둘은 장소 유형 식별용 문자열이라 원본에 보고한 것이 닫혀야 풀린다.
- **채팅 발신자 컨텍스트 메뉴가 한 번도 배선되지 않은 것을 찾아 원본에 적었다.** `ChatPlayerService`가 209줄인데 `new`가 어디에도 없고 기본 키를 읽는 분기도 없다. 이름은 단축키 목록에 나오는데 누르면 아무 일도 안 일어나고, 원본 릴리스 노트는 이것을 기능으로 적었다. 기준 다섯을 다 넘겼다.
- **달라무드 빌드 참조를 15.0.3.5로 올렸다.** 두 버전으로 다 대조해 1,379건 중 없는 타입과 멤버가 양쪽 모두 0건이라 깨지는 것은 없었지만, 문서가 어셈블리 버전이 오르면 새로 만들라고 정하고 공식 배포도 마침 15.0.3.5라 **요구하는 해시 대조를 같은 버전끼리 할 수 있는 때**였다. 넷이 공식과 바이트까지 같은 것을 다시 쟀고, 한국 전용 둘의 해시가 설치본 호환 패치 파일의 값과 각각 맞는다 — 대조가 우리 계산만으로 닫히지 않는다.
- **원본 `v6.08.31`을 얹었다.** 태그 둘을 한 번에 건너뛰었고 덧댄 규칙이 하나도 없다 — 원본이 더한 새 문장이 전부 파서 안으로 들어와서 못 읽음이 열 곳 그대로다. 「못 읽음을 미적용과 같은 무게로 본다」는 지침이 생긴 뒤 처음으로 그 숫자가 안 움직인 버전이다.
- **원본이 문장 다섯을 고쳐 고아가 둘에서 일곱이 됐다.** 시전 안내 설정 이름이 뜻이 넓어지며 `Gegner wirkt Aktion`에서 `Zauber-Ansagen`이 됐고, 토벌 대상 계수 문장 넷에 토벌수첩 단계가 들어갔다. 다섯을 새 쌍으로 옮겨 고아가 둘로 돌아왔다 — **고아는 대장이 낡았다는 신호이지 결함이 아니고, 그 신호가 없었으면 다섯 문장이 조용히 영어로 나갔다.**
- **캐릭터 창 넷째 탭 이름을 758~761 블록에서 찾으면 틀린다.** 그 블록은 능력치·프로필·클래스/잡까지는 탭 차례 그대로인데 넷째가 `화폐`라 독일어 `Ansehen`과 뜻이 안 맞는다. 실제 이름은 102512행 `평판`이고, 뒤따르는 `플레이어 추천`과 `부족 우호도`가 그 탭의 내용이라는 것이 근거다. **연속된 블록이 끝까지 같은 목록이라고 단정하면 그럴듯한 낱말이 답이 된다.**
- **게임패드 버튼 이름 다섯을 못 찾아 모드가 짓는 말로 뒀다.** 네모·세모·동그라미·엑스·십자키가 Addon 전 행에서 0건이고, 게임패드 설정 블록(4101~4118)도 `게임패드 버튼 유형`과 `버튼 할당`까지만 글로 적을 뿐 버튼 이름은 그림으로만 표시한다. 초코보 기술 계통도 같은 부류다 — 축사 블록이 세 갈래를 `방어 역할`·`공격 역할`·`회복 역할`로 부르지만 그 묶음을 가리키는 이름이 없다. 둘 다 `not_found`에 근거를 남겼고 **귀 판정에서 바뀔 수 있다.**
- **원본 `v6.08.32`를 얹었다.** 태그 하나, 커밋 하나다. 덧댄 규칙이 없고 못 읽음은 열 곳 그대로다. 원본이 새로 낸 문장 서른하나가 미적용으로 잡혔다 — 마수도감과 마수 분류, 파티 모집 조건 창, 길드 의뢰 분류, 이동 소리 설정, 가루다 임무 설정의 문장이다. 전부 옮겨 미적용이 여든둘로 돌아왔다.
- **원본이 문장 둘을 고쳐 고아가 둘에서 넷이 됐다.** 이벤트가 없을 때의 안내에서 요괴워치 문장이 빠졌고, 소리 확인 안내에 부딪힘 알림음과 높낮이 알림음이 끼었다. 둘을 새 쌍으로 옮겨 고아가 둘로 돌아왔다.
- **Kante를 `턱`이 아니라 `높낮이`로 옮겼다.** 한 음절이라 귀로 흘리기 쉽다. 게임에 이 낱말이 없어 `부딪힘`·`미계약`·`세부 지역`과 함께 `not_found`에 근거를 남겼고 **귀 판정에서 바뀔 수 있다.**
