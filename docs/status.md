# 현황

**이 파일은 120줄을 넘지 않는다.** 지금 상태와 열린 작업만 담는다. 끝난 것은 `changelog.md`, 결정과 그 까닭은 `decisions.md`, 결함 하나하나는 `issues/W-NN.md`가 갖는다.

기존 저장소의 현황판이 1,748줄이 된 까닭은 여는 규칙만 있고 지우는 규칙이 없어서다. 여기서는 **표의 한 줄이 자기 파일을 가리키고, 닫히면 그 파일을 지운다.**

## 지금

원본 v5.95를 조립해서 빌드까지 선다. 아직 릴리스를 내지 않았다.

| | |
|---|---|
| 원본 판 | v5.95 (`upstream/` 서브모듈) |
| 우리 커밋이 원본에 얹힌 것 | 0개 |
| 빌드 | `dotnet build -c Release` 경고 0개, 오류 0개 |

## 번역 잔량 — 이것이 기준선이다

**2026-09-04, v5.95에서 조립 도구가 처음 낸 값이다.** 이 뒤의 증감은 전부 여기에 대고 잰다. 기준선을 틀리게 잡으면 그 뒤의 판정이 전부 어긋난다.

| 이름 | 값 | 뜻 |
|------|-----|-----|
| 적용 | 817곳 (대장 793행) | 대장의 한국어가 실제로 들어간 자리 |
| 고아 | 5행 | 대장에는 있는데 소스에서 못 만났다. 업스트림이 그 문장을 고쳤다는 신호 |
| 미적용 | 13곳 | 영어로 나가고 모드가 로그에 남긴다 |
| 못 읽음 | 43곳 | 갈림길인 줄은 알지만 파서 손 밖이라 위 숫자에 없다 |
| 튜플 사전 | 3,023건 | 조립 도구가 아예 보지 않는다 |

**판정은 잔량으로 한다.** "몇 행 반영"이 아니라 **영어로 나가는 자리가 몇 곳인가**를 본다. 지금은 13 + 43 + 3,023이다.

고아 5행은 `Aethernet`·`FATEs`·`Place`·`Levequests` 둘이다. 업스트림이 고친 문장이라 대장을 맞춰야 한다.

## 열린 작업

| 번호 | 무엇 | 상태 |
|------|------|------|
| — | 빌드·동기화 워크플로 둘 | 미착수 |
| — | 조립 산출물이 게임에서 실제로 도는지 | 미확인. 정식 자리에 깔고 귀로 판정해야 한다 |
| — | 미번역 로그가 인게임에서 찍히는지 | 미확인. 게임 안에서만 되는 판정이다 |
| — | 설치 프로그램과 실행기 | 별도 단계로 미룸 |
| — | 캐릭터 생성 묘사 3,023건 | 방식부터 정해야 한다 |
| — | 고아 5행 | 업스트림이 고친 문장에 대장을 맞춘다 |

번호를 딴 결함은 `issues/`에 파일로 있다. 지금은 없다.

## 원본에 보낼 것

`upstream-reports/`에 파일 넷이 있다. 셋은 재포팅에서 버린 개선분이고 하나는 새로 찾은 원본 결함이다.

## 못 푼 것

**KR Dalamud 참조를 CI에서 어떻게 받을지가 반만 정해졌다.** 방식은 확정했고(`docs/dev/dalamud-refs.md`) 로컬 빌드는 되지만, 이 저장소에 그 릴리스를 아직 안 만들었다.

**교차 리뷰 결과를 종결하지 못하고 있다.** `codex-review`를 손으로 돌리면 `.json`만 남고 `.md`가 안 생기는데, 종결 스크립트가 `.md`를 요구한다. 이번 재포팅에서 돌린 리뷰 다섯이 미처결로 남아 있다.
