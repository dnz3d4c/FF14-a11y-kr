# 프로젝트 지침 — FF14-a11y-kr

전역 지침(`~/.claude/CLAUDE.md`)에 더해 이 저장소에만 적용된다.

## 현황판이 먼저다 [필수]

기능 구현·수정·개선을 **시작하기 전에** [docs/status.md](docs/status.md)를 연다. 남은 일의 기준이고 다른 문서는 근거만 갖는다.

- **120줄 상한이 있다.** `tools/docs-check`가 검사한다. 넘치면 줄이 아니라 무엇을 지울지를 정한다 — 끝난 것은 [changelog.md](docs/changelog.md), 결정과 그 까닭은 [decisions.md](docs/decisions.md), 결함 하나하나는 `docs/issues/W-NN.md`가 갖는다.
- **숫자를 손으로 옮겨 적으면 `tools/docs-check`가 산출물과 대조한다.** 어긋난 채로 두면 검사가 빨개진다. 조립이 낼 때마다 바뀌는 값이라 갱신을 미루지 않는다.

## 원본 취급 [필수]

`upstream/`은 원본 저장소의 서브모듈이고 **태그를 그대로 가리킨다.** 원본 위에 우리 커밋을 얹지 않으므로 rebase도 충돌도 없다.

- **원본의 결함을 만나면 우리가 고치지 않는다. 기록하고 원본에 보고한다.** 남기는 자리는 [upstream-reports/](upstream-reports/)이고, 무엇을 안 보내는지의 기준은 그 안의 `rejected.md`가 갖는다 — 만들기 전에 그것부터 본다.
- **한국어 문장은 소스가 아니라 `korean/strings.json`에서 고친다.** C# 쪽은 조립 산출물이다.
- 원본 파일을 다루는 방식이 셋이고 이름이 뜻을 말한다 — `kr/`(원본에 없는 파일), `replace/`(통째 대체), `graft/rules.json`(자리 삽입). 규칙과 실패 조건은 [docs/dev/assemble.md](docs/dev/assemble.md).
- **`replace/`가 위험한 부류다.** 원본의 개선을 조용히 덮을 수 있다. 두 줄을 고치려고 파일 하나를 통째로 가져오지 않는다.

## 파서가 못 읽는 자리를 미적용과 같은 무게로 본다 [필수]

조립 보고의 「못 읽음」은 **영어로 나가면서 미적용에도 안 잡히는 자리다.** 세지도 못하고 고치지도 못하므로 신호가 없다.

2026-09-04에 교차 리뷰가 그 부류에서 둘을 짚었다 — 비행 종료 안내가 높낮이 조건에서만, 지역 이동 안내가 단복수 삼항 탓에 영어로 나가고 있었다. **보고가 이름을 대고 있었는데도 사람이 안 봤다.**

삼항을 갈래 밖으로 빼거나 배열을 원소별로 펴면 대장이 읽는다. 선례가 `graft/rules.json`에 여럿 있다.

## 사용자에게 실행을 시킬 때 [필수]

**먼저, 시킬 일인지부터 본다.** 빌드·조립·검사·발행은 전부 내가 돌린다. 사용자에게 남기는 것은 **게임 안에서만 되는 것**뿐이다 — 인게임 조작과 **귀로 하는 판정**.

- **"평소대로", "알아서", "필요하면" 금지.** 실행할 것은 정확히 하나를 제시한다.
- **실행 가능한 것만 준다.** zip·폴더·빌드 산출물 경로를 실행하라고 주지 않는다.
- 경로는 **윈도 표기**(백슬래시). 세션 셸이 bash라고 bash 표기로 주지 않는다.
- 명령을 준 뒤 **무엇을 보고 성공을 판단하는지**와 **나에게 뭘 알려줘야 하는지**를 같이 적는다.

### 깔고 재는 법

    uv run python tools/pack/pack.py
    dist\FF14AccessibilityInstaller-KR.exe --install --skip-vnavmesh

**개발용 자리에 깔지 않는다.** 정식 자리와 상호 배타적이라 같은 모드가 두 번 적재되고, 무엇보다 **들리는 것과 낸 것이 갈라져 귀 판정이 통째로 무효가 된다.** 게임이 떠 있으면 `Tolk.dll`을 잡고 있어서 반만 깔리므로 끈 상태에서 돌린다.

**Debug로 빌드하지 않는다.** 원본 csproj의 `DeployToDevFolder`가 Debug 조건이라 결과가 개발용 자리로 자동 복사된다.

## 발행 [필수]

절차와 게이트와 탈출구는 [docs/dev/release.md](docs/dev/release.md)가 갖는다. 여기 베끼지 않는다.

- **`gh`를 부르기 전에 게이트를 다 끝낸다.** 바깥에 흔적이 남으면 되돌리는 길이 없다.
- **올린 것을 도로 받아 잰다.** 자산이 반만 올라가도 `gh`는 0으로 끝나고, 받는 쪽에는 오류가 아니라 "새 판이 없다"로 보인다.
- **탈출구로 넘긴 것은 화면에 남긴다.** 조용히 지나가면 잰 판과 안 잰 판이 같아 보인다.

## 새 워크플로는 반드시 밀어 본다 [필수]

**러너 안의 사실과 저장소 밖의 사실은 로컬 실증으로 원리적으로 안 잡힌다.** 현황판의 「CI에서만 드러난 결함」이 그 목록이고 여섯 건이다 — 트리거 브랜치 이름, 러너 파이썬 인코딩, 저장소의 Actions 권한 설정, PR 체크아웃의 ref, .NET SDK 경로, 게임 없는 러너의 설치 실증.

문법이 맞으면 YAML도 유효하고 `tools/ci-check`도 통과한다. 한 번 밀어 보고 결과를 확인한다.

## 문서를 쓸 때

- 사용자가 읽는 문서(가이드·설치 안내·사용법)는 [ko-user-guide 스킬](.claude/skills/ko-user-guide/SKILL.md)이 규칙을 갖는다. 파판14 공식 가이드의 형식과 문체를 따른다.
- 모드가 **말하는** 문장은 [ko-localization 스킬](.claude/skills/ko-localization/SKILL.md)이다. 명사형·해라체이고 사람이 읽는 문서와 반대다.
- 릴리스 노트는 [ko-release-notes 스킬](.claude/skills/ko-release-notes/SKILL.md)과 [release-notes-rules.md](docs/dev/release-notes-rules.md)다. `tools/notes-check`가 N1~N26을 잰다.
- **이름은 지어내지 않는다.** 게임 용어의 원천은 `korean/terms.json`이고, 없으면 `not_found`에 근거를 남긴다. 못 찾았으면 **못 찾았다고 적고 묻는다.**

## 나머지는 문서가 소유한다

여기에 베끼지 않는다. 어긋나면 문서가 맞다.

- 조립 절차와 도구 명세: `docs/dev/assemble.md`
- 발행: `docs/dev/release.md`
- 빌드 참조: `docs/dev/dalamud-refs.md`
- 설치 프로그램: `docs/dev/installer.md`
- 결정과 그 까닭: `docs/decisions.md`
